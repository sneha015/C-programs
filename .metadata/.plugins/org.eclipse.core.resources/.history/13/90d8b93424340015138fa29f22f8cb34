#include<iostream>
#include "hashT.h"

using namespace std;

int main()
{
	hashT<int> ht(19);
	int k[6]={115, 153, 586, 208, 985, 111};
	bool found;
	int loc =4;

	for(int i=0; i< 6 ; i++)
	{
		cout<<k[i]%19<<": "<<k[i]<<" ";
		ht.insert(k[i] % 19 , k[i]);
	}
	cout<<"\n";
	ht.print();

	ht.search(loc,k[loc],found);
	if(found == true)
		cout<<"found!! \n";
	else
		cout<<"element not found \n";



	return 0;
	system.exit(0);
}






